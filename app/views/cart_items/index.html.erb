<div class="container">
  <p class="Back-Button"><%= link_to 'Back', 'javascript:history.back()' %> </p>
  <h5 class="MenuCategory-Title Cart">Cart</h5>
  <% if !@cart_items.empty? %>
    <p class="Right"><%= link_to 'Delete Cart', destroy_cart_path,  method: :delete, data: { confirm: 'Are you sure?' } %></p>
    <%=render partial:"items", locals: {cart:@cart,cart_items:@cart_items }%>
    <%= link_to "<button class='card_checkout_btn'>Confirm Order</button>".html_safe,edit_cart_item_path(@cart.id)%>
  <% else %>
    <div class="empty_cart">
      <h5>Your cart is empty
      </h5>
    </div>
  <% end %>
</div>
